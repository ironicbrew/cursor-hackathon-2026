{
  "buildCommand": "npm run build",
  "devCommand": "vite --port $PORT",
  "installCommand": "npm install",
  "outputDirectory": "dist",
  "framework": "vite",
  "functions": {
    "api/inngest.ts": {
      "maxDuration": 60
    }
  }
}
